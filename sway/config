# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.

# put the following to bashrc
#export XDG_CURRENT_DESKTOP=sway # xdg-desktop-portal
#export XDG_SESSION_DESKTOP=sway # systemd
#export XDG_SESSION_TYPE=wayland
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
#exec create_output
#exec swaymsg output "HEADLESS-1" resolution 1600x900 # dont work anyways
#exec /home/rodhfr/Code/createmonitor.sh 
#exec input-remapper-gtk 




exec sunshine
exec wayvnc ::
exec gsettings set org.gnome.desktop.interface color-scheme prefer-dark
exec waybar
exec audiorelay_run
#exec sunshine
exec /usr/bin/lxpolkit
#exec --no-startup-id dunst
exec_always "~/.config/sway/start_waybar.workstation"
#exec_always --no-startup-id nwg-panel
exec_always --no-startup-id swaync
exec_always --no-startup-id autotiling 
#exec mpv --input-ipc-server=/tmp/mpvsocket --idle
exec mpv-remote-app linux123

# displays
#output HEADLESS-1 position -1600 0
#output HDMI-A-1 pos 0 0 resolution 1920x1080@74.973Hz
output HDMI-A-1 pos 0 0 resolution 1920x1080@60Hz
output eDP-1 pos 1920 0
# for 4k display:
# $ swaymsg output HDMI-A-1 scale 3



workspace 1 output HDMI-A-1
workspace 2 output HDMI-A-1
workspace 3 output HDMI-A-1
workspace 4 output HDMI-A-1
workspace 5 output HDMI-A-1
workspace 5 output HDMI-A-1
workspace 6 output HDMI-A-1
workspace 7 output HDMI-A-1
workspace 8 output HDMI-A-1
workspace 9 output eDP-1
workspace 10 output HEADLESS-1

#exec ~/.azotebg
set $wallpapers_path ~/.config/Wallpapers
output * bg `find $wallpapers_path -type f | shuf -n 1` fill

# exec --no-startup-id vncserver :2 -geometry 1920x1080 -depth 16 -localhost no

# gaps
gaps inner 10
gaps outer 10

# Definir o tamanho da borda
default_border pixel 5
for_window [class=".*"] border 2

# windows rules
assign [class="BoltLauncher"] workspace 1
assign [class="net-runelite-client-RuneLite"] workspace 1
#assign [app_id="one.ablaze.floorp"] workspace 2
#assign [app_id="org.gnome.Nautilus"] workspace 4
for_window [app_id="mpv"] sticky enable, floating enable, resize set width 400px height 225px, move position 1500 10



# Acomodação das janelas
smart_gaps on


set $mod mod1
set $term ghostty
# set $term_id term
#set $t_pos floating enable, border none, move position center, resize set width 100 ppt height 100 ppt

#for_window [app_id="com.mitchellh.ghostty"] move scratchpad
# bindsym --to-code F1 exec ...
#bindsym $mod+Ctrl+g exec swaymsg [app_id=ghostty] scratchpad show || swaymsg move [app_id=ghostty] to scratchpad
#exec $term


# input config
#input type:keyboard {
#    xkb_layout us,br
#    xkb_options grp:rctrl_toggle
#}
input * xkb_options "compose:menu"

input "1267:12447:ELAN1200:00_04F3:309F_Touchpad" {
    tap enabled
}

input "9610:42:SINO_WEALTH_Gaming_KB__Mouse" {
    accel_profile "flat"
    pointer_accel 1
}


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 12

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex-autostart --autostart --environment sway

# Wayland Locking
#exec --no-startup-id swaylock -f

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is the most common system tray GUI for it.
# For sway, you might want to use sway's waybar or use NetworkManager's applet directly
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# #bindsym $mod+m output "HDMI-A-1" enable ; output "eDP-1" toggle
# # modo sala de tv:
# bindsym $mod+m output "eDP-1" disable ; output "HDMI-A-1" scale 3 ; exec --no-startup-id nwg-panel
# # modo desktop (HDMI-A-1 on ; eDP-1 off)
# bindsym $mod+Ctrl+m output "eDP-1" disable ; output "HDMI-A-1" enable ; output "HDMI-A-1" scale 1 ; exec --no-startup-id nwg-panel
# # modo desktop (eDP-1 on ; HDMI-A-1 on)
# bindsym $mod+Shift+m output "eDP-1" enable ; output "HDMI-A-1" enable ; output "HDMI-A-1" scale 1 ; exec --no-startup-id nwg-panel
# # only laptop
# bindsym $mod+Ctrl+s output "eDP-1" enable ; output "HDMI-A-1" disable ; output "HEADLESS-1" disable ; exec --no-startup-id nwg-panel
#
# # modo sunshine (physical disabled)
# bindsym $mod+s exec --no-startup-id sh ~/.config/sway/check_headless.sh ; output "HDMI-A-1" disable ; output "eDP-1" disable ; output "HEADLESS-1" resolution 1600x900 ; output "HEADLESS-1" scale 1.5 ; exec --no-startup-id nwg-panel
# # $ swaymsg output "HEADLESS-1" resolution 1600x720

# modo sala de tv:
bindsym $mod+m output "eDP-1" disable ; output "HDMI-A-1" scale 3 
#; exec --no-startup-id pkill waybar && sleep 1 && waybar
# modo desktop (HDMI-A-1 on ; eDP-1 off)
bindsym $mod+Ctrl+m output "eDP-1" disable ; output "HDMI-A-1" enable ; output "HDMI-A-1" scale 1 
#; exec --no-startup-id pkill waybar && sleep 1 && waybar
# modo desktop (eDP-1 on ; HDMI-A-1 on)
bindsym $mod+Shift+m output "eDP-1" enable ; output "HDMI-A-1" enable ; output "HDMI-A-1" scale 1 
#; exec --no-startup-id pkill waybar && sleep 1 && waybar
# only laptop
bindsym $mod+Ctrl+s output "eDP-1" enable ; output "HDMI-A-1" disable ; output "HEADLESS-1" disable 
#; exec --no-startup-id pkill waybar && sleep 1 && waybar

# modo sunshine (physical disabled)
bindsym $mod+Shift+s exec --no-startup-id sh ~/.config/sway/check_headless.sh ; output "HDMI-A-1" disable ; output "eDP-1" disable ; output "HEADLESS-1" resolution 1600x720 ; output "HEADLESS-1" scale 1.2 
#; exec --no-startup-id pkill waybar && sleep 1 && waybar

bindsym XF86MonBrightnessUp exec brightnessctl set 10%+
bindsym XF86MonBrightnessDown exec brightnessctl set 10%-
bindsym $mod+g exec brightnessctl set 1 && pkill dim
bindsym $mod+Shift+g exec dim -d 0 -p -a 0.4
bindsym $mod+Ctrl+g exec pkill dim  


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
#tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+q kill

# start rofi (a program launcher)
bindsym $mod+x exec --no-startup-id rofi -show drun -theme dmenu

# cycle containers
#bindsym $mod+Tab focus next
#bindsym $mod+Shift+Tab focus prev

# change focus
# bindsym $mod+h focus left
# bindsym $mod+j focus down
# bindsym $mod+k focus up
# bindsym $mod+l focus right
bindsym $mod+a focus left
bindsym $mod+w focus up 
bindsym $mod+d focus right
bindsym $mod+s focus down


# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
#bindsym $mod+b split h 

# split in vertical orientation
bindsym $mod+v split v
bindsym $mod+Shift+v split h

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen toggle
#bindsym $mod+f exec flatpak run one.ablaze.floorp 
bindsym $mod+f exec firefox
bindsym $mod+Ctrl+f exec ~/.config/sway/floatingfox
bindsym $mod+Ctrl+k exec ~/.config/sway/HotkeyKitty
bindsym $mod+e exec nautilus
bindsym $mod+n exec swaync-client -t
bindsym $mod+t exec macopa -Fywy
bindsym $mod+Shift+t exec macopa -Sywy
bindsym $mod+Shift+p exec grim -g "$(slurp)" ~/Screenshots/screenshot_$(date +%Y%m%d%H%M%S).png 
bindsym $mod+p exec grim -g "$(slurp)" - | wl-copy
#bindsym $mod+Shift+p exec grim -g "$(slurp -p)" -t ppm - | convert - -format '%[pixel:p{0,0}]' txt:- | tail -n 1 | cut -d ' ' -f 4 | wl-copy
bindsym $mod+k floating toggle, sticky disable
bindsym $mod+Ctrl+p sticky enable, floating enable, resize set width 400px height 225px, move position 1500 10
bindsym $mod+Ctrl+r sticky enable, floating enable, resize set width 400px height 225px, move position 10 180
bindsym $mod+Ctrl+i sticky enable, floating enable, resize set width 400px height 225px, move position 300 10
#bindsym $mod+Ctrl+r sticky toggle, floating toggle, resize set width 350px height 225px, move position 10 180


# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle
#
# focus the parent container
#bindsym $mod+a focus parent

# Define names for default workspaces for which we configure key bindings later on.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1 ; workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2 ; workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3 ; workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4 ; workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5 ; workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6 ; workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7 ; workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8 ; workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9 ; workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10 ; workspace $ws10

# silently move to workspace
bindsym $mod+Ctrl+1 move container to workspace $ws1
bindsym $mod+Ctrl+2 move container to workspace $ws2
bindsym $mod+Ctrl+3 move container to workspace $ws3
bindsym $mod+Ctrl+4 move container to workspace $ws4
bindsym $mod+Ctrl+5 move container to workspace $ws5
bindsym $mod+Ctrl+6 move container to workspace $ws6
bindsym $mod+Ctrl+8 move container to workspace $ws8
bindsym $mod+Ctrl+9 move container to workspace $ws9
bindsym $mod+Ctrl+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart sway (preserves your layout/session, can be used to upgrade sway)
bindsym $mod+Shift+r restart
# exit sway
bindsym $mod+Shift+e exec "swaymsg exit"

#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+minus scratchpad show
#

bindsym $mod+r mode "resize"
# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym l resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym h resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# # Start swaybar (or use your own bar config here)
# bar {
#     swaybar_command waybar
#     position top
#     waybar
#     mode hide     
#     ##toggle with super key
#     modifier Mod4
#     #mode invisible
#
# }

bar {
    #swaybar_command waybar
    position top
    mode invisible 
    #modifier Mod4
}


